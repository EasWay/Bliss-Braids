import MobileServiceGrid from '@/components/services/MobileServiceGrid';

export const metadata = {
  title: 'Services - Bliss Braids',
  description: 'Browse our professional braiding services with transparent pricing',
};

export default function MobileServicesPage() {
  return <MobileServiceGrid />;
}
